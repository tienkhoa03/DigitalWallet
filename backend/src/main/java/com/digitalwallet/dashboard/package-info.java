/**
 * Dashboard module — FR3.x (admin live metrics + fraud alert fan-out).
 * WebSocket auth per .claude/rules/security.md §2: JWT validated before upgrade accepted.
 * Layering — subpackages api/, ws/, consumer/.
 */
package com.digitalwallet.dashboard;
