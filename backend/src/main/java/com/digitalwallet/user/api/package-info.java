/**
 * JAX-RS resources for the {@code user/} module — signup and login.
 *
 * <p>Path constants are exposed as inner {@code public static final String} fields per
 * {@code .claude/rules/backend_coding.md §2}. Resources return DTO records, never JPA
 * entities.
 */
package com.digitalwallet.user.api;
