/**
 * JPA entity and Panache repository for the {@code "user"} table.
 *
 * <p>This package is JPA-only. JAX-RS imports here are a defect per
 * {@code .claude/rules/backend_coding.md §1}; cross-feature repositories are imported
 * only through the owning module's service layer.
 */
package com.digitalwallet.user.persistence;
